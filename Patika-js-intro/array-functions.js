const names = ["enes", "ahmet", "mehmet"];

const students = [
  {
    name: "enes",
    age: 2,
  },

  {
    name: "ahmet",
    age: 3,
  },
];

// push
// names.push("ayşe");

// map
// names.map((name) => {
//   console.log(name);
// });

//find
// const nameToFind = students.find((student) => student.name == "enes");
// console.log(nameToFind);

//filter
// const filtered = students.filter((student) => student.age < 10);
// console.log(filtered);

// some ->  returns true or false at least 1 mets conditions

// every -> returns boolean if all elements mets conditions

// includes -> return boolean, if elements included in array
